name: Test

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:

  run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - id: foo
      uses: hashi-strawb/tfc-ephemeral-workspace-check@test
      with:
        TFE_TOKEN: ${{ secrets.TFE_TOKEN }}